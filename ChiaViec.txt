✅ FE1 (tăng workload) — Auth + Public + Portal Học viên
FE1 làm:
Auth


Login / Register / Forgot / First change password / Role redirect


Trang Khách (Public)


Home / Courses List / Course Detail / Register Course / About / Teachers / Contact


Portal Học viên (5.1 → 5.6) — CHUYỂN TỪ FE4 SANG FE1


Dashboard học viên


Lịch học & lớp học (read)


Điểm danh & xin nghỉ (request form + trạng thái)


Kết quả học tập (điểm/nhận xét)


Học phí (read-only + upload minh chứng nếu có)


Hồ sơ cá nhân + đổi mật khẩu


Vì Portal Học viên chủ yếu là read-only + form request, rất “khớp” với FE1 (public + form flow).
FE1 KHÔNG làm Enroll Requests backoffice nữa (để tránh FE1 quá rộng). Enroll Requests chuyển cho FE2.

✅ FE2 — Vận hành trung tâm (Classes + Rooms/Schedule + Attendance + Notifications + Enroll Requests backoffice)
FE2 làm:
Quản lý lớp (Admin+Giáo vụ) + class detail tabs


Phòng học & lịch


Điểm danh (Giáo vụ + GV)


Thông báo


Enroll Requests (Backoffice): list/chi tiết/đổi trạng thái/tạo học viên/gán lớp dự kiến


Enroll Requests gắn chặt “xếp lớp – vận hành”, đưa cho FE2 hợp lý nhất.

✅ FE3 — Admin backbone (Dashboard + Courses + Reports + Settings/Audit)
Giữ nguyên:
Dashboard Admin + việc cần xử lý


Quản lý khóa học


Báo cáo – thống kê (chuyên cần/kết quả/doanh thu+công nợ) + export UI


Cấu hình phân quyền + audit log



✅ FE4 (giảm tải) — People + Kế toán + Portal Giảng viên
FE4 làm:
Quản lý học viên (Admin + Giáo vụ theo quyền)


Quản lý giảng viên


Quản lý nhân viên


Module Kế toán (dashboard + phiếu thu + công nợ + báo cáo tài chính)


Portal Giảng viên: dashboard + lớp đang dạy (read) + nhập điểm & nhận xét


(FE2 vẫn làm điểm danh + sổ đầu bài + tài liệu)



So sánh nhanh sau khi cân
FE1: Auth + 7 trang public + ~6 trang portal học viên ⇒ tăng đáng kể


FE2: Core vận hành + enroll backoffice ⇒ vẫn nặng nhưng “core”


FE3: Admin backbone ⇒ vừa


FE4: People + Accounting + Teacher portal ⇒ giảm rõ rệt so với trước



Bản “chốt” cuối cùng (ngắn gọn)
FE1: Auth + Public + Student Portal


FE2: Classes + Rooms/Schedule + Attendance + Notifications + Enroll Requests (Backoffice)


FE3: Admin Dashboard + Courses + Reports + Settings/Audit


FE4: Students/Teachers/Staff management + Accounting + Teacher Portal (grades/feedback)



Chi tiết:

0) Trang chung (Auth)
Đăng nhập


Đăng ký


Quên mật khẩu (OTP/link UI)


Đổi mật khẩu lần đầu (tuỳ chọn)


Điều hướng theo role sau login


1) Trang Khách (Public / Visitor)
Home (Trang chủ)


Courses List (tìm kiếm + filter + sort)


Course Detail


Register Course (Form đăng ký khóa học)


About


Teachers (Public)


Contact (kèm form gửi câu hỏi)


2) Module “Đơn đăng ký / Yêu cầu ghi danh” (Enroll Requests)
Vì bạn có “khách đăng ký khóa học”, cần trang xử lý ở phía hệ thống.
Backoffice list đơn đăng ký (Admin & Giáo vụ dùng)


Chi tiết đơn


Cập nhật trạng thái: Mới → Tiếp nhận → Xếp lớp → Đã đóng phí → Hoàn tất


Hành động: “Tạo hồ sơ học viên từ đơn” (UI trigger) + “Gán lớp dự kiến” (UI)


Tổng FE1: Auth + 7 trang public + module Enroll Requests (cả public + backoffice) → workload lớn, nhưng rất “tròn” và độc lập.

FE2 — Module Lớp học + Lịch/Phòng + Điểm danh + Thông báo
1.3 / 2.2 Quản lý lớp (Classes) (Admin + Giáo vụ)
Danh sách lớp + lọc trạng thái


Thêm/Sửa/Xóa lớp (Admin có xóa, Giáo vụ không)


Chi tiết lớp (tabs):


Thông tin lớp (course, sĩ số, trạng thái…)


Lịch học


Phòng / link online


Gán giảng viên


Danh sách học viên trong lớp (add/remove/transfer/reserve/drop UI)


Buổi học (sessions)


1.7 / 2.5 Quản lý phòng học & lịch (Rooms/Schedule)
CRUD phòng


Lịch sử dụng phòng (calendar hoặc list theo tuần)


Cảnh báo trùng phòng/trùng lịch (UI warning)


2.3 / 4.3 Điểm danh (Attendance) (Giáo vụ + Giảng viên)
List buổi học theo lớp


Mở buổi → điểm danh


Chỉnh sửa điểm danh (Giáo vụ có quyền sửa)


Báo cáo học viên vắng nhiều


2.6 Thông báo (Nội bộ + học viên)
Gửi thông báo:


theo lớp


theo học viên


nhiều học viên trong lớp


Inbox/History thông báo đã gửi (list + chi tiết)


Tổng FE2: 4 module “core vận hành” → workload nặng tương đương FE1.

FE3 — Module Khóa học + Báo cáo học tập + Cấu hình & Nhật ký hệ thống + Dashboard Admin
1.1 Dashboard Admin
Widget: tổng học viên / lớp / doanh thu / công nợ / vắng học / việc cần xử lý


Trang “Thông báo hệ thống/việc cần xử lý” (list)


1.2 Quản lý khóa học (Courses) (Admin)
Danh sách khóa học


Thêm/Sửa/Xóa khóa học


Chi tiết khóa học


(Tuỳ chọn) gói học phí (UI)


1.9 Báo cáo – thống kê (Admin)
Báo cáo chuyên cần theo lớp


Báo cáo kết quả học tập (điểm test)


Báo cáo doanh thu + công nợ (tổng hợp view, FE3 làm UI; dữ liệu chi tiết do FE4/FE2/FE3 dùng API)


Export Excel/PDF (UI button)


1.10 Cấu hình hệ thống & nhật ký (Admin)
Quản lý phân quyền (UI map role → permission) (mức UI, backend quyết định)


Nhật ký hoạt động (audit log): list + filter theo user/action/date


Tổng FE3: “Admin backbone”: Dashboard + Courses + Reports + Settings/Audit → cân tốt.

FE4 — People + Kế toán + Portal Giảng viên + Portal Học viên
(Để cân bằng, FE4 gánh nhiều trang nhưng mỗi trang có form/table tương tự, dễ tái sử dụng)
A) 1.4 Quản lý học viên (Admin) + 2.4 (Giáo vụ xem/nhập theo quyền)
Danh sách học viên + filter trạng thái


Thêm/Sửa/Xóa (Admin), Giáo vụ có thể tạo/cập nhật nếu cho phép


Chi tiết học viên:


lịch sử lớp


chuyên cần


kết quả học tập


tình trạng (đang học/nghỉ/bảo lưu)


B) 1.5 Quản lý giảng viên (Admin)
Danh sách + CRUD


Chi tiết giảng viên + lịch dạy


(Tuỳ chọn) cấu hình lương theo giờ/buổi/lớp (UI)


C) 1.6 Quản lý nhân viên (Admin)
Danh sách + CRUD


Gán role (Giáo vụ/Kế toán)


Khóa/mở/reset mật khẩu


D) 3) Kế toán (Full module)
3.1 Dashboard kế toán
Tổng thu hôm nay/tháng


DS nợ


Phiếu thu chờ xác nhận (nếu có)


Thống kê theo lớp


3.2 Phiếu thu / học phí
Tạo phiếu thu


Update trạng thái thanh toán


In/xuất phiếu (UI)


3.3 Công nợ
List công nợ + filter theo lớp/hạn


(Tuỳ chọn) nhắc nợ (tạo notification record UI)


3.4 Thu chi nội bộ (tuỳ chọn)
CRUD khoản chi


Báo cáo thu-chi


3.5 Báo cáo tài chính
Doanh thu theo ngày/tháng/năm


Theo lớp/khóa


Công nợ tổng/quá hạn


Export


E) Portal Giảng viên (4.1, 4.2, 4.4, 4.5)
Dashboard giảng viên


Xem lớp đang dạy + chi tiết lớp (read)


Sổ đầu bài / nội dung buổi học (nếu FE2 đã làm sổ đầu bài thì FE4 chỉ làm phần nhập điểm/nhận xét; để tránh overlap, chốt như dưới)


Nhập điểm & nhận xét


Tổng FE4: People + Accounting + Teacher/Student portals → khối lượng lớn nhưng cân bằng nhờ tái sử dụng UI và module hóa.
